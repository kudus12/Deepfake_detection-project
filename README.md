# Deepfake Detection Project

This project implements a Flask-based web interface and machine learning preprocessing scripts for deepfake detection.

## Dataset Note
The real and fake video datasets are very large and are stored locally.  
A `.gitignore` file is used to ensure datasets are NOT uploaded to GitHub.  
Only the source code and system implementation files are included in this repository.
